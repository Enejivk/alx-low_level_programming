#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"
/**
 * free_listint2- This function free the linklist and return NULL
 * @head: A pointer to the first head pointer
 */
void free_listint2(listint_t **head)
{
	if (head == NULL || *head == NULL)d
}
