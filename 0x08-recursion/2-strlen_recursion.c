#include "main.h"
/**
* _strlen_recursion - recursion to return string length
* @s: This is the pointer to the string to be determined
*Return: It returns an integer variable
*/
int _strlen_recursion(char *s)
{
if (*s == '\0')
{
return (0);
{
else
{
(return 1) + _strlen_recursion(s + 1);
}
}

