#include <stdio.h>
/**
 * main - this is the entry point of the code
 *
 * Return: return is always 0
 */
int main(void)
{
	int v;

	for (v = 0; v <= 10; v++)
	{
		putchar ('0' + v);
	}
		putchar('\n');
return (0);
}
