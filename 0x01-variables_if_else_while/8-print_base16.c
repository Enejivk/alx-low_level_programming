#include <stdio.h>
/**
 * main - this is the entry point of the code
 *
 * Return: return is always 0
 */
int main(void)
{
	int v;

	for (v = 48; v < 58; v++)
	{
		putchar (v);
	}
	for (v =97; v < 103; v++)
	{
		putchar (v);
	}
		putchar('\n');
return (0);
}
